# 安全策略

简体中文 | [English](docs/en/SECURITY.md)

## 支持版本

当前项目仍处于早期版本阶段。安全修复默认面向主干和最新发布版本；历史版本是否回补由维护者根据影响范围决定。

## 漏洞报告

请不要在公开 issue 中披露未修复漏洞细节。优先通过 GitHub Private Vulnerability Reporting 报告；如果仓库暂未启用该功能，请先创建不包含细节的公开占位 issue，或联系维护者建立私有沟通渠道后再发送完整细节。

报告建议包含：

- 受影响模块、版本、依赖坐标或提交号。
- 复现步骤或最小复现样例。
- 影响评估，例如数据泄露、任意文件访问、可利用的反序列化、权限绕过、远程触发的拒绝服务、远程执行或一致性破坏。
- 受影响部署条件，例如网络暴露面、认证状态、集群规模、传输方式、ADB/JDBC 或状态机插件路径。
- 已知缓解方式。

## 不适合公开 issue 的内容

以下类型请走私有安全报告渠道：

- 可导致未授权读写、数据泄露或权限提升的问题。
- 可触发任意文件访问、任意代码执行或命令执行的问题。
- 可被远程稳定触发的拒绝服务、资源耗尽或一致性破坏。
- 涉及密钥、令牌、生产配置、私有仓库地址或供应链凭据的泄露。
- 需要利用细节、漏洞证明或攻击载荷才能说明的问题。

## 响应流程

维护者收到报告后应：

- 确认报告有效性和影响范围。
- 给出修复计划或临时缓解建议。
- 在修复发布后公开安全公告和升级建议。

## 凭据与发布安全

- 不得提交 `signing.properties`、GPG 私钥、仓库令牌、OSS/Maven 发布账号密码或生产配置。
- 发布凭据应放在用户级 Gradle 配置、环境变量或专用密钥管理系统中。
- 如果凭据曾出现在日志、截图、聊天记录或仓库历史中，应立即轮换。
